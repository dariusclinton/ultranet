<h3>Commenter</h3>

{{ form_start(form, {'attr': {'class': ''}}) }}

{{ form_errors(form) }}

<div class="form-group">
  {{ form_label(form.texte, 'Message', {'attr': {'class': 'control-label'}}) }}
  {{ form_widget(form.texte, {'attr': {'class': 'form-control'}}) }}
  {{ form_errors(form.texte, {'attr': {'class': 'alert alert-danger'}}) }}
</div>

{{ form_rest(form) }}

<div class="form-group">
  <input type="submit" value="Publier" class="btn btn-success">
</div>

{{ form_end(form) }}

